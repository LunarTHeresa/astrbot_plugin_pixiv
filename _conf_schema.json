{
  "pixiv_refresh_token": {
    "description": "Pixiv refresh_token",
    "type": "string",
    "hint": "使用 get_pixiv_token_manual.py 获取后粘贴到这里",
    "obvious_hint": true
  },
  "allow_r18": {
    "description": "是否允许 R18 指令",
    "type": "bool",
    "default": false,
    "hint": "开启后可使用 /pixr 和 /novelr"
  },
  "pixiv_proxy": {
    "description": "Pixiv 代理（可选）",
    "type": "string",
    "default": "",
    "hint": "例如 http://127.0.0.1:7890；服务器无法直连 Pixiv 时填写"
  },
  "request_timeout_sec": {
    "description": "请求超时秒数",
    "type": "int",
    "default": 30,
    "hint": "网络较慢可调大，例如 60"
  },
  "send_image_as_file": {
    "description": "图片按文件中转发送（推荐开启）",
    "type": "bool",
    "default": true,
    "hint": "开启后插件先通过代理下载图片再发送，可避免 QQ 侧直连 pximg 超时"
  },
  "prefer_tag_search": {
    "description": "优先按标签精确检索",
    "type": "bool",
    "default": true,
    "hint": "开启后先按标签 exact_match_for_tags 搜索，再回退到模糊匹配"
  }
}
